# tuple is immutable
# When we use this? suppose we want a set of values which should not be changed, then we need to use tuple instead of list
# Iteration in tuple is faster then list
a = (21, 32, 43, 54)

print(a[1])
